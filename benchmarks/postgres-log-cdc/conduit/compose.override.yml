services:
  benchi-conduit:
    volumes:
      # mount path is relative to the location of the original docker-compose file
      - ../../benchmarks/postgres-log-cdc/conduit/pipeline.yml:/app/pipelines/pipeline.yml
